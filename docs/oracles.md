# What compares what

Every artefact this emitter produces, and what would catch it disagreeing with the thing it has to
agree with.

The two defects that reached a consumer were the same shape: two artefacts generated by one program,
compared by nothing. This page exists so that a pair with no comparator is visible before a consumer
finds it, rather than after.

Each row was checked by planting a defect and confirming the named arm goes red.

## Emitted artefacts

| artefact                             | must agree with                                  | what catches a disagreement                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------ | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `schemas.gen.ts`                     | the OpenAPI document openapi3 publishes          | `conformance/differential.test.ts`, two independent ways: describers in `conformance/shape.ts`, and `z.toJSONSchema()` compared against the document with nothing of ours in between                                                                                                                                                                                     |
| `schemas.gen.ts`                     | the document at **every graded OpenAPI version** | `conformance/differential.test.ts` runs the whole comparison again at 3.2.0 and asserts the divergence set is IDENTICAL to 3.1.0's, so a newer document finding a new disagreement fails. One compile emits both; `GRADED_OPENAPI_VERSIONS` states the list once                                                                                                         |
| documents being read at all          | the layout openapi3 actually writes              | `documentsRead`, floored. openapi3 relocates output into per-version subdirectories once more than one version is requested, and a reader left on the flat layout finds nothing - which is not an error. `compileScenario` also empties its directory, after a control passed green against the previous run's files                                                     |
| `schemas.gen.ts`                     | values on the wire                               | `acceptance.test.ts` parses conformant and non-conformant payloads; `reference/roundtrip.test.ts` serves two APIs nobody here designed                                                                                                                                                                                                                                   |
| `schemas.gen.ts`                     | **the document's VERDICT on a value**            | `conformance/behaviour.test.ts` puts one value through Ajv 2020-12 on the document and through the emitted validator, over the whole corpus, and requires the same answer. The two axes above compare DESCRIPTIONS and can report perfect agreement about a pair that answers differently the moment a value arrives                                                     |
| `schemas.gen.ts`                     | the TypeScript compiler                          | `emit.test.ts` runs `tsc` over the emitted directory under `strict`, for an ordinary service and for a cyclic one                                                                                                                                                                                                                                                        |
| `requests.gen.ts`                    | `schemas.gen.ts`                                 | `contracts/agreement.test.ts` compares how both name every parameter of every operation                                                                                                                                                                                                                                                                                  |
| `requests.gen.ts`                    | `schemas.gen.ts`, for models                     | `wire-contract.gen.ts`, compiled by `emit.test.ts`: it asserts `z.infer` of each validator is identical to the contract type                                                                                                                                                                                                                                             |
| `vocabularies.gen.ts`                | the enums the spec declares                      | transitively, and confirmed by planting a defect: the emitted Zod imports these tuples, so a wrong member changes what the validator accepts and reddens `acceptance.test.ts`, the optionality arm, and the emitted-output compile                                                                                                                                       |
| `wire-contract.gen.ts`               | itself, as TypeScript                            | `emit.test.ts`; a failed assertion is a compile error, so compiling it IS the check                                                                                                                                                                                                                                                                                      |
| `schemas.gen.ts`, `requests.gen.ts`  | that they parse and typecheck at all             | `emit.test.ts` compiles four distinct shapes: an ordinary service, a reference cycle, identifiers carrying a `$`, and reserved-word model names with an envelope union. Each was added after a shape that did not compile reached a published version                                                                                                                    |
| a route's `requestContentTypes`      | the media types `@typespec/http` resolves        | `mediatypes/mediatypes.test.ts`, with a floor requiring the fixture to declare a body offering more than one                                                                                                                                                                                                                                                             |
| a parameter validator                | the string a server actually hands over          | `wire/params.test.ts`, over plain scalars, named scalars, literal unions, optional and nullable wrappers and flattened lists                                                                                                                                                                                                                                             |
| `reservedPathParameters`             | `allowReserved` as `@typespec/http` resolves it  | `parameters/reserved.test.ts`, twice and neither by re-reading the flag: the `+` operator in `uriTemplate` tokenised on the RFC 6570 grammar, and openapi3's own `path-reserved-expansion` count. Floors on both classes, since an implementation returning every path parameter agrees with an all-reserved fixture                                                     |
| a reserved path parameter            | NOT the document, deliberately                   | `parameters/reserved.test.ts` pins the divergence instead: openapi3 strips the operator at every version, and no published path carries one. See `docs/reference.md`                                                                                                                                                                                                     |
| every diagnostic                     | naming a remedy, and pointing at a declaration   | `diagnostics/located.test.ts`, asserted over the declared set rather than the ones that were short                                                                                                                                                                                                                                                                       |
| one `@service` per emitted directory | not overwriting another service                  | `services/services.test.ts`, both that two services separate and that one does not move                                                                                                                                                                                                                                                                                  |
| every tracked file                   | being ASCII                                      | `portability.test.ts`, naming file, line and codepoint, because the character that matters is the one nobody can see                                                                                                                                                                                                                                                     |
| `schemas.gen.ts`                     | **the Zod API Zod still recommends**             | `deprecations.test.ts`, which reads the `@deprecated` markers out of the INSTALLED Zod rather than keeping a list. `vocabulary.test.ts` asks whether a construct is derivable from the document; this asks whether the emitter is writing the spelling the library still stands behind. The receiver decides, not the name: `.url()` is retired and `z.url()` is current |
| every emitted file                   | ASCII, no absolute paths, no attribution         | `portability.test.ts`, `attribution.test.ts`, `provenance.test.ts`                                                                                                                                                                                                                                                                                                       |

## The published surface

| thing                                     | must agree with                       | what catches a disagreement                                                                                            |
| ----------------------------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| `package.json` `exports`                  | what `dist/` actually contains        | `packaging.test.ts` packs and inspects the tarball                                                                     |
| `DEFAULT_RUNTIME_MODULE`                  | a subpath this package publishes      | `packaging.test.ts`                                                                                                    |
| names the output imports from `./runtime` | what `runtime.ts` exports             | `packaging.test.ts`                                                                                                    |
| `src/` imports                            | declared peer dependencies            | `packaging.test.ts`                                                                                                    |
| the README install line                   | whether `./runtime` exports a value   | `documentation.test.ts`                                                                                                |
| `docs/reference.md`                       | every diagnostic and option in `$lib` | `documentation.test.ts`, both directions: an undocumented one fails, and a declared diagnostic with no call site fails |
| `CHANGELOG.md`                            | `package.json`'s version              | `documentation.test.ts`                                                                                                |
| `README.md`                               | that `docs/` is reachable             | `documentation.test.ts`                                                                                                |
| `test/reference/service.tsp`              | the vendored copy in `typespec-hono`  | `vendored.test.ts` against the sha256 in `test/reference/PROVENANCE.md`                                                |

## Known gaps

- **A metadata-carrying model used as both a payload root and a nested body** loses its header property
  where the document keeps it. `isPayloadProperty` takes an `ignoreMetadataAnnotations` argument that
  openapi3 threads as emitter context and this does not pass. It raises `metadata-ignored` from
  `@typespec/http`, which fires zero times across the corpus.
- **`format` is compared by neither axis**, deliberately: it is an annotation rather than an
  assertion, and a class assertion in `vocabulary.test.ts` refuses any format-derived Zod call, so
  enabling one breaks a test rather than moving a number.
- **4 negotiated response bodies** cannot be attributed to a single arm, and **4 reduce to no readable
  kind** on one side. Both are pinned counts rather than floors.
- **Two emitted declarations may share one identifier, and nothing compares that.** Two models of the
  same name in different namespaces are published by openapi3 as `A.Shared` and `B.Shared`, and this
  emitter declares `Shared` twice, which does not compile. The differential's
  `component-name-collision` arm does not catch it: that arm iterates the DOCUMENT's components and
  fires when two of them claim one identifier, whereas this is two emitter declarations from types
  the document names apart. Reproduced, not yet fixed. `parameters/body-optionality` and
  `parameters/spread` carry the same shape through spread-source naming.
- **A probe is only as good as the instance it mutates.** `behaviour.test.ts` builds the smallest
  value the document accepts and mutates one fact at a time; where it cannot build one - an `allOf`
  whose members would have to be merged, a pattern its sampler cannot draw from - the component is
  counted as unbuildable rather than probed with a value that means nothing. The count is floored
  from the other side, so a change that quietly stops producing instances fails.
- **`emit.test.ts` compiles four fixtures rather than the whole corpus.** Every shape that reached a
  published version uncompiled was found by adding one; the general form is a corpus-wide `tsc` pass.

## How a new pair gets a comparator

An artefact added here without a row above is a pair nobody is comparing. The rule that produced this
page: for each thing the emitter writes, name the thing it must agree with, then plant a defect and
confirm something goes red. If nothing does, that is the next defect, and a consumer will find it.
