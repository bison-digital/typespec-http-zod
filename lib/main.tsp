// The emitter library itself — diagnostics and its options schema. There are no decorators: every
// one this package's ancestor defined said something the published OpenAPI document could not, which
// is the whole reason they were deleted. See `src/lib.ts` and `src/tsp-index.ts`.
import "../dist/src/tsp-index.js";
